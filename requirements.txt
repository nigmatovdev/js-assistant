# Install PyTorch with CUDA FIRST (before the rest):
#   pip install torch --index-url https://download.pytorch.org/whl/cu121
#
# Then install everything else:
#   pip install -r requirements.txt

sentence-transformers
chromadb
fastapi
uvicorn[standard]
python-dotenv
